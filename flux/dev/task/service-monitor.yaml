apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: app-msc-dipterv-dev-task
  labels:
    application: app-msc-dipterv-dev-task
spec:
  endpoints:
    - interval: 1m
      path: metrics
      port: http
  namespaceSelector:
    matchNames:
    - app-msc-dipterv-dev
  selector:
    matchLabels:
      application: task
