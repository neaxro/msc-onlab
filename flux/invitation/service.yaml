---
apiVersion: v1
kind: Service
metadata:
  name: invitation
  labels:
    application: invitation
spec:
  selector:
    app: app-msc-dipterv-dev-invitation
  ports:
    - name: http
      protocol: TCP
      port: 5003
      targetPort: http
