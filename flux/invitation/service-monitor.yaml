apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: app-msc-dipterv-dev-invitation
  labels:
    application: app-msc-dipterv-dev-invitation
spec:
  endpoints:
    - interval: 1m
      path: metrics
      port: http
  namespaceSelector:
    matchNames:
    - app-msc-dipterv-dev
  selector:
    matchLabels:
      application: invitation
