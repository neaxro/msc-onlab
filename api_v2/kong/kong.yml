_format_version: '2.1'

services:
  - name: auth_service
    url: http://auth:5000/
    routes:
      - name: auth_route
        paths:
          - /auth

  - name: keycloak_service
    url: http://keycloak:8080
    routes:
      - name: keycloak_route
        paths:
          - /keycloak
